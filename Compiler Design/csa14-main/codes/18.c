#include<stdio.h>

int main()
{
    printf("TRAILING Sets:\n");
    printf("TRAILING(E) = { ), id }\n");
    printf("TRAILING(T) = { ), id }\n");
    printf("TRAILING(F) = { ), id }\n");
    return 0;
}